# TeamAI - Профиль пользователя с редактированием

## ✅ Реализовано

### ProfileScreen (Четвертая вкладка)
Полнофункциональный профиль с возможностью редактирования навыков и опыта работы.

**Файлы:**
- `lib/screens/profile/profile_screen.dart` - основной экран
- `lib/screens/profile/edit_profile_modal.dart` - модальное окно редактирования

---

## 🎨 Дизайн и функциональность

### Основной экран профиля

#### Информация о пользователе:
- **Аватар** - круглая иконка с градиентом
- **Имя** - полное имя пользователя
- **Роль/Должность** - например "Full Stack Developer"
- **Email** ✉️ - видимый email для поиска в команду
- **Кнопка "Редактировать профиль"** - открывает модальное окно

#### Статистика (3 карточки):
- **15 Задач** - количество выполненных задач
- **3 Команды** - количество команд
- **92% Эффективность** - процент выполнения

#### Опыт работы:
- **Специальный блок** с иконкой портфеля
- Формат: "Опыт: 3 года"
- Автоматическое склонение (год/года/лет)

#### Навыки:
- **Заголовок** + счетчик навыков
- **Chips** с навыками
- Возможность редактирования
- Пустое состояние: "Добавьте ваши навыки в профиле"

#### Секции:
- **Мои команды** - список команд с цветными карточками
- **Настройки** - уведомления, конфиденциальность, язык, помощь, выход

---

### Модальное окно "Редактировать профиль"

#### Header:
- Кнопка закрытия (X)
- Заголовок "Редактировать профиль"
- Кнопка "Готово" (сохранить)

#### Форма:

**1. Имя** *
- TextField для ввода имени
- Обязательное поле

**2. Роль/Должность** *
- TextField для должности
- Placeholder: "Например: Full Stack Developer"
- Обязательное поле

**3. Email** * ✉️
- TextField для email
- Иконка email слева
- Подсказка: "По этой почте вас смогут найти и добавить в команду"
- Валидация на корректность email
- Обязательное поле

**4. Опыт работы (лет)** *
- TextField для числа
- Suffix: "лет"
- Подсказка: "ИИ использует эту информацию для оптимального распределения задач"
- Только цифры
- Обязательное поле

**5. Навыки**
- Заголовок + счетчик навыков
- Подсказка: "ИИ анализирует ваши навыки для назначения подходящих задач"
- TextField для добавления навыка
- Кнопка "+" для добавления
- Список навыков (chips) с кнопкой удаления (x)
- Пустое состояние с иконкой лампочки

---

## 🔑 Ключевые особенности

### 1. Email видимый и важный
- Email **всегда видим** в профиле
- Иконка ✉️ для акцента
- Подсказка: "По этой почте вас смогут найти и добавить в команду"
- Валидация при редактировании

### 2. Редактирование навыков
- ✅ Добавление новых навыков
- ✅ Удаление существующих навыков
- ✅ Подсказка о важности для AI
- ✅ Пустое состояние

### 3. Редактирование опыта
- ✅ Число лет опыта
- ✅ Подсказка о важности для AI распределения
- ✅ Правильное склонение (год/года/лет)
- ✅ Отображение в отдельном блоке

### 4. Валидация формы
- Проверка всех обязательных полей
- Валидация email (должен содержать @)
- Успешное уведомление после сохранения
- Ошибки в SnackBar

### 5. State Management
- Локальный state для данных пользователя
- Обновление UI после сохранения
- TODO: интеграция с API

---

## 📊 Структура данных

### User Profile
```dart
String _userName         // "Алексей Иванов"
String _userRole         // "Full Stack Developer"
String _userEmail        // "alexey.ivanov@example.com"
List<String> _skills     // ["Flutter", "Dart", "React", ...]
int _experienceYears     // 3
```

---

## 🎯 Использование для поиска в команду

### Workflow добавления пользователя:

1. **Создание проекта в Calendar**
   - Пользователь заполняет форму "Добавить задачу"
   - Нажимает "Создать задачу с помощью ИИ"

2. **Поиск участников по email**
   ```dart
   // В форме создания проекта (TODO):
   TextField(
     decoration: InputDecoration(
       hintText: 'Введите email участника',
       prefixIcon: Icon(Icons.search),
     ),
     onChanged: (email) async {
       // API call: GET /api/users/search?email=alexey.ivanov@example.com
       final users = await searchUsersByEmail(email);
       // Показать результаты
     },
   )
   ```

3. **Результаты поиска**
   - Показываются пользователи с похожим email
   - Отображаются: аватар, имя, роль, email, навыки, опыт
   - Кнопка "Добавить в проект"

4. **AI анализирует добавленных участников**
   ```json
   {
     "users": [
       {
         "email": "alexey.ivanov@example.com",
         "name": "Алексей Иванов",
         "role": "Full Stack Developer",
         "skills": ["Flutter", "Dart", "React", "Node.js"],
         "experienceYears": 3
       }
     ]
   }
   ```

5. **AI распределяет задачи**
   - На основе навыков
   - С учетом опыта
   - Автоматически назначает исполнителей

---

## 🤖 Как AI использует данные профиля

### 1. Навыки (Skills)
```dart
// Пример: задача требует ["Flutter", "UI/UX"]
Task task = Task(
  title: "Создать UI компоненты",
  requiredSkills: ["Flutter", "UI/UX"],
);

// AI находит пользователей с этими навыками
List<User> candidates = users.where((user) {
  return user.skills.contains("Flutter") && 
         user.skills.contains("UI/UX");
}).toList();

// AI выбирает лучшего кандидата
User bestCandidate = AI.selectBestCandidate(candidates, task);
```

### 2. Опыт работы (Experience Years)
```dart
// AI учитывает опыт при оценке времени выполнения
int estimateTaskHours(Task task, User user) {
  int baseHours = task.complexity * 8;
  
  // Опытные сотрудники - меньше времени
  double experienceMultiplier = 1.0;
  if (user.experienceYears < 1) {
    experienceMultiplier = 1.5; // +50% времени
  } else if (user.experienceYears > 5) {
    experienceMultiplier = 0.7; // -30% времени
  }
  
  return (baseHours * experienceMultiplier).round();
}
```

### 3. Email для поиска
```dart
// API endpoint для поиска
GET /api/users/search?email={query}

// Поиск по частичному совпадению
SELECT * FROM users 
WHERE email LIKE '%alexey%' 
   OR email LIKE '%ivanov%';

// Результат:
[
  {
    "id": "1",
    "name": "Алексей Иванов",
    "email": "alexey.ivanov@example.com",
    "role": "Full Stack Developer",
    "skills": ["Flutter", "Dart", "React"],
    "experienceYears": 3,
    "avatarUrl": null
  }
]
```

---

## 🎨 UI компоненты

### Skill Chip
```dart
Container(
  padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
  decoration: BoxDecoration(
    color: Color(0xFF4F9CF9).withValues(alpha: 0.1),
    borderRadius: BorderRadius.circular(20),
    border: Border.all(color: Color(0xFF4F9CF9).withValues(alpha: 0.3)),
  ),
  child: Row(
    children: [
      Text(skill),
      Icon(Icons.close), // В режиме редактирования
    ],
  ),
)
```

### Experience Badge
```dart
Container(
  padding: EdgeInsets.symmetric(horizontal: 20, vertical: 12),
  decoration: BoxDecoration(
    color: Color(0xFF4F9CF9).withValues(alpha: 0.1),
    borderRadius: BorderRadius.circular(12),
    border: Border.all(color: Color(0xFF4F9CF9).withValues(alpha: 0.3)),
  ),
  child: Row(
    children: [
      Icon(Icons.work_outline, color: Color(0xFF4F9CF9)),
      SizedBox(width: 8),
      Text('Опыт: 3 года'),
    ],
  ),
)
```

---

## 📝 TODO для production

### Backend API

```
# User endpoints
GET    /api/users/:id              # Получить профиль
PUT    /api/users/:id              # Обновить профиль
GET    /api/users/search           # Поиск по email
POST   /api/users/:id/skills       # Добавить навык
DELETE /api/users/:id/skills/:name # Удалить навык

# Profile data
{
  "id": "user_123",
  "name": "Алексей Иванов",
  "email": "alexey.ivanov@example.com",
  "role": "Full Stack Developer",
  "skills": ["Flutter", "Dart", "React", "Node.js"],
  "experienceYears": 3,
  "avatarUrl": "https://...",
  "stats": {
    "tasksCompleted": 15,
    "teamsCount": 3,
    "efficiency": 92
  }
}
```

### State Management
- Provider / Riverpod для глобального state
- Сохранение данных пользователя
- Кэширование профиля
- Автоматическое обновление UI

### Улучшения UI
- [ ] Загрузка аватара
- [ ] Crop и resize изображения
- [ ] Предложения навыков (autocomplete)
- [ ] Категории навыков
- [ ] Рейтинг навыков (1-5 звезд)
- [ ] Сертификаты и достижения
- [ ] История изменений профиля

### Функционал поиска
- [ ] Поиск пользователей по email в форме создания проекта
- [ ] Фильтры: по навыкам, опыту, роли
- [ ] Превью профиля перед добавлением
- [ ] Массовое добавление участников
- [ ] Приглашения по email

---

## 🔐 Безопасность

### Email уникальность
- Email должен быть уникальным в системе
- Проверка при регистрации
- Валидация при обновлении

### Приватность
- Возможность скрыть email от других
- Настройки видимости профиля
- Блокировка нежелательных приглашений

---

## 💡 Примеры использования

### Сценарий 1: Редактирование профиля

1. Открыть Profile tab
2. Нажать "Редактировать профиль"
3. Изменить имя, роль, email
4. Обновить опыт работы (5 лет)
5. Добавить новые навыки: "TypeScript", "GraphQL"
6. Удалить ненужные навыки
7. Нажать "Готово"
8. Увидеть уведомление "Профиль успешно обновлен"

### Сценарий 2: Поиск и добавление в команду

1. Пользователь А создает проект
2. В форме вводит email: "alexey@example.com"
3. Система находит "Алексей Иванов"
4. Показывает профиль:
   - Full Stack Developer
   - Опыт: 3 года
   - Навыки: Flutter, Dart, React, Node.js
5. Пользователь А добавляет в проект
6. AI анализирует и распределяет задачи

### Сценарий 3: AI подбирает исполнителя

```
Задача: "Разработать REST API"
Требуемые навыки: ["Node.js", "Database"]

AI ищет:
1. Алексей Иванов - Node.js ✓, 3 года опыта
2. Мария Петрова - Node.js ✓, Database ✓, 5 лет опыта ⭐

AI выбирает: Мария Петрова
Reasoning: "Мария имеет все требуемые навыки и больший опыт"
```

---

## 🎉 Итоги

### Реализовано:
- ✅ Отображение профиля с email
- ✅ Кнопка редактирования
- ✅ Модальное окно с формой
- ✅ Редактирование имени, роли, email
- ✅ Редактирование опыта работы
- ✅ Добавление/удаление навыков
- ✅ Валидация формы
- ✅ Уведомления об успехе
- ✅ Пустые состояния
- ✅ Правильное склонение
- ✅ Подсказки для AI

### Готово к интеграции:
- Backend API для профилей
- Поиск пользователей по email
- AI анализ навыков и опыта
- Добавление в команды

### Email для поиска:
- ✅ Видимый в профиле
- ✅ Редактируемый
- ✅ С валидацией
- ✅ С подсказкой о назначении
- Готов для поиска через API

---

**Профиль готов к использованию! 🎉**

Пользователи могут:
1. Редактировать свои данные
2. Управлять навыками
3. Указывать опыт работы
4. Быть найденными по email
5. Получать задачи на основе AI анализа

---

*Создано: 5 ноября 2025*
*Версия: 1.0.0*
