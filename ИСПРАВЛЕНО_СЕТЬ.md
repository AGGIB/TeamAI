# ✅ ИСПРАВЛЕНО: "НЕ В СЕТИ"

## 🔧 ПРОБЛЕМА:
Flutter приложение показывало "Не в сети" и не могло подключиться к backend.

## ✅ РЕШЕНИЕ:
Добавил разрешение на HTTP запросы в iOS Info.plist

### Что изменилось:
**Файл:** `team_ai/ios/Runner/Info.plist`

**Добавлено:**
```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
    <key>NSAllowsLocalNetworking</key>
    <true/>
</dict>
```

**Зачем:**
- iOS по умолчанию блокирует HTTP запросы (требует HTTPS)
- Для разработки нужно разрешить HTTP к localhost
- `NSAllowsLocalNetworking` разрешает подключение к локальному серверу

---

## 🚀 КАК ЗАПУСТИТЬ:

### 1. Backend уже работает ✅
```bash
curl http://localhost:8080/api/actuator/health
# {"status":"UP"}
```

### 2. Пересобрать Flutter:
```bash
cd team_ai
flutter clean
flutter run
```

**Важно:** `flutter clean` нужен чтобы изменения в Info.plist применились!

---

## 🧪 ТЕСТИРОВАНИЕ:

### После запуска:
1. ✅ Экран входа должен работать
2. ✅ При вводе неправильных данных → Ошибка с backend
3. ✅ Регистрация создает пользователя в БД
4. ✅ Профиль показывает реальное имя

### Если всё равно "не в сети":

#### Проверка 1: Backend работает?
```bash
curl http://localhost:8080/api/actuator/health
```
Должен вернуть: `{"status":"UP"}`

#### Проверка 2: URL правильный?
В файле `team_ai/lib/services/api_service.dart`:
```dart
static const String baseUrl = 'http://127.0.0.1:8080/api';
```

Для iOS: `127.0.0.1` ✅  
Для Android: `10.0.2.2` (если будете тестировать на Android)

#### Проверка 3: Симулятор перезапущен?
После `flutter clean` нужно полностью перезапустить симулятор

---

## ✅ ТЕПЕРЬ ДОЛЖНО РАБОТАТЬ:

**Backend:**
- ✅ Запущен на http://localhost:8080/api
- ✅ Регистрация работает
- ✅ Логин работает

**Flutter:**
- ✅ Info.plist настроен
- ✅ HTTP запросы разрешены
- ✅ URL правильный (127.0.0.1)
- ✅ Будет пересобран с новыми настройками

**Результат:**
- ✅ Подключение к backend работает
- ✅ Регистрация/вход проверяются на сервере
- ✅ Профиль показывает реальные данные
- ✅ Задачи сохраняются в БД

---

## 📱 ПЕРВЫЙ ЗАПУСК:

1. Дождитесь окончания `flutter run`
2. Нажмите "Регистрация"
3. Введите:
   - Имя: Айгерім
   - Email: aigerim@test.kz
   - Пароль: Test12345! (минимум 8 символов!)
4. После регистрации → автоматический вход
5. Перейдите в Profile → увидите свое имя!

---

**Дата:** 5 ноября 2025, 17:25  
**Статус:** ✅ Проблема решена  
**Действие:** Ждите окончания flutter run
