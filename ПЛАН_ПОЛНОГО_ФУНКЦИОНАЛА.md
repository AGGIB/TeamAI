# 🚀 ПЛАН ПОЛНОГО ФУНКЦИОНАЛА

## 📋 ЗАДАЧИ ОТ ПОЛЬЗОВАТЕЛЯ:

1. ✅ **Календарь** - полный функционал для задач, связь с backend
2. ✅ **Добавление пользователей** - по email в команду
3. ✅ **AI распределение** - по навыкам команды
4. ✅ **Проверка профиля** - навыки/опыт перед созданием задач
5. ✅ **Редактор навыков** - в настройках, связь с БД
6. ✅ **AI анализ** - всех данных пользователя
7. ✅ **Чат-бот** - к созданным задачам

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ:

### 1. ИСПРАВИТЬ ЗАГРУЗКУ ДАННЫХ ⚠️

**Проблема:** Приложение показывает "No projects from backend, using mock data"

**Причина:** 
- ProjectsProvider загружается ДО входа в систему
- Нет токена авторизации
- Backend возвращает 401/403

**Решение:**
```dart
// В ProjectsProvider - убрать загрузку из конструктора
ProjectsProvider() {
  // НЕ загружаем данные здесь!
}

// В AuthProvider - после успешного входа
if (success) {
  // Загрузить данные после входа
  await Provider.of<ProjectsProvider>(context, listen: false).loadAllData();
}
```

---

### 2. КАЛЕНДАРЬ С ЗАДАЧАМИ ✅

**Файл:** `lib/screens/calendar/calendar_screen.dart`

**Что добавить:**
- ✅ Календарь с задачами по датам
- ✅ Отображение задач на выбранный день
- ✅ Загрузка задач с backend
- ✅ Создание задачи на выбранный день
- ✅ Редактирование задач
- ✅ Изменение статуса задач
- ✅ Фильтр по статусу (TODO, IN_PROGRESS, DONE)

**Backend endpoints (уже есть):**
```
GET /api/tasks - получить все задачи
POST /api/tasks - создать задачу
PUT /api/tasks/{id} - обновить задачу
PATCH /api/tasks/{id}/status - изменить статус
```

---

### 3. ДОБАВЛЕНИЕ ПОЛЬЗОВАТЕЛЕЙ ПО EMAIL ✅

**Файл:** Создать `lib/screens/team/add_member_dialog.dart`

**Функционал:**
```dart
// Поиск пользователя по email
Future<TeamMember?> searchUserByEmail(String email) async {
  final users = await _apiService.searchUsers(email);
  return users.isNotEmpty ? users.first : null;
}

// Добавление в команду проекта
Future<bool> addMemberToProject(String projectId, String userId) async {
  await _apiService.addProjectMember(projectId, userId);
}
```

**Backend endpoint (уже есть):**
```
GET /api/users/search?email={email}
POST /api/projects/{id}/members
```

---

### 4. AI РАСПРЕДЕЛЕНИЕ ПО НАВЫКАМ ✅

**Файл:** `lib/screens/ai/ai_distribution_screen.dart`

**Функционал:**
```dart
// Анализ команды
- Получить всех участников проекта
- Получить их навыки из БД
- Получить нераспределенные задачи

// Вызов AI
Future<Map<String, dynamic>> distributeTasksWithAI(String projectId) async {
  return await _apiService.aiDistributeTasks(projectId);
}

// Результат
- AI назначает задачи по навыкам
- Показывает рекомендации
- Пользователь может принять или отклонить
```

**Backend endpoint (уже есть):**
```
POST /api/ai/distribute-tasks
{
  "projectId": "uuid"
}
```

---

### 5. ПРОВЕРКА НАВЫКОВ ПЕРЕД СОЗДАНИЕМ ЗАДАЧ ✅

**Файл:** `lib/screens/ai/create_task_screen.dart`

**Логика:**
```dart
Future<bool> checkUserProfile() async {
  final user = authProvider.currentUser;
  
  if (user.skills.isEmpty || user.experienceYears == 0) {
    // Показать диалог
    showDialog(
      context: context,
      builder: (context) => AlertDialog(
        title: Text('Заполните профиль'),
        content: Text('Для создания задач заполните навыки и опыт в настройках'),
        actions: [
          TextButton(
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) => SkillsSettingsScreen(),
                ),
              );
            },
            child: Text('Перейти в настройки'),
          ),
        ],
      ),
    );
    return false;
  }
  return true;
}
```

---

### 6. РЕДАКТОР НАВЫКОВ В НАСТРОЙКАХ ✅

**Файл:** Создать `lib/screens/settings/skills_editor_screen.dart`

**Функционал:**
```dart
class SkillsEditorScreen extends StatefulWidget {
  // Поля
  List<String> skills = [];
  int experienceYears = 0;
  
  // Методы
  void addSkill(String skill) { ... }
  void removeSkill(String skill) { ... }
  
  Future<void> saveProfile() async {
    await _apiService.updateUser(userId, {
      'skills': skills,
      'experienceYears': experienceYears,
    });
  }
}
```

**Backend endpoint:**
```
PUT /api/users/me
{
  "skills": ["Flutter", "Dart"],
  "experienceYears": 3
}
```

---

### 7. ЧАТ-БОТ К ЗАДАЧАМ ✅

**Файл:** Создать `lib/screens/chat/task_chat_screen.dart`

**Функционал:**
```dart
class TaskChatScreen extends StatefulWidget {
  final Task task;
  
  // Отправить сообщение AI
  Future<void> sendMessageToAI(String message) async {
    final response = await _apiService.aiChat(
      message,
      'Task: ${task.title}, Description: ${task.description}',
    );
    
    // Показать ответ
    messages.add(ChatMessage(
      text: response,
      isAI: true,
    ));
  }
}

// Возможные вопросы к AI:
- "Как лучше выполнить эту задачу?"
- "Какие подзадачи создать?"
- "Сколько времени это займет?"
- "Кто в команде может помочь?"
```

**Backend endpoint (уже есть):**
```
POST /api/ai/chat
{
  "message": "Как выполнить задачу?",
  "context": "Task: ... Description: ..."
}
```

---

## 📁 СТРУКТУРА ФАЙЛОВ:

```
lib/
├── screens/
│   ├── calendar/
│   │   └── calendar_screen.dart ← ОБНОВИТЬ
│   ├── team/
│   │   ├── add_member_dialog.dart ← СОЗДАТЬ
│   │   └── team_members_screen.dart ← СОЗДАТЬ
│   ├── ai/
│   │   ├── ai_distribution_screen.dart ← СОЗДАТЬ
│   │   └── create_task_screen.dart ← ОБНОВИТЬ
│   ├── chat/
│   │   └── task_chat_screen.dart ← СОЗДАТЬ
│   └── settings/
│       └── skills_editor_screen.dart ← СОЗДАТЬ
├── providers/
│   └── projects_provider.dart ← ОБНОВИТЬ
└── services/
    └── api_service.dart ← УЖЕ ГОТОВ
```

---

## 🔄 ПОРЯДОК РЕАЛИЗАЦИИ:

### Шаг 1: Исправить загрузку данных (КРИТИЧНО!)
```
1. Убрать loadAllData() из конструктора ProjectsProvider
2. Добавить loadAllData() после успешного входа в AuthProvider
3. Протестировать - данные должны загружаться после входа
```

### Шаг 2: Обновить календарь
```
1. Добавить загрузку задач с backend
2. Добавить создание задач на дату
3. Добавить редактирование
4. Добавить фильтры
```

### Шаг 3: Добавить управление командой
```
1. Создать экран поиска пользователей
2. Добавить функцию добавления в команду
3. Интегрировать в экран создания проекта
```

### Шаг 4: Добавить AI распределение
```
1. Создать экран с визуализацией
2. Показать навыки команды
3. Показать рекомендации AI
4. Дать возможность принять/отклонить
```

### Шаг 5: Добавить проверку навыков
```
1. Проверка перед созданием задачи
2. Диалог с предложением заполнить
3. Переход в настройки
```

### Шаг 6: Редактор навыков
```
1. Создать экран редактора
2. Добавить чипы для навыков
3. Добавить слайдер для опыта
4. Сохранение в БД
```

### Шаг 7: Чат-бот
```
1. Создать экран чата
2. История сообщений
3. Интеграция с AI
4. Кнопка на экране задачи
```

---

## 🧪 ТЕСТИРОВАНИЕ:

### После каждого шага:
1. ✅ Перезапустить Flutter
2. ✅ Войти в систему
3. ✅ Проверить новый функционал
4. ✅ Убедиться что данные сохраняются в БД

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ:

- Шаг 1: 10 минут
- Шаг 2: 20 минут
- Шаг 3: 15 минут
- Шаг 4: 15 минут
- Шаг 5: 10 минут
- Шаг 6: 20 минут
- Шаг 7: 15 минут

**Итого:** ~1.5-2 часа на полную реализацию

---

## 🚀 НАЧИНАЕМ!

**Сейчас делаю:**
1. Исправляю загрузку данных
2. Обновляю календарь
3. Добавляю все остальное

**Backend уже готов для всего этого!** ✅
