# 📱 НАСТРОЙКА ДЛЯ РЕАЛЬНОГО iPhone УСТРОЙСТВА

## ✅ ЧТО УЖЕ ИСПРАВЛЕНО

### Backend URL изменен:
```dart
// Было (работает только для симулятора):
static const String baseUrl = 'http://127.0.0.1:8080/api';

// Стало (работает для реального устройства):
static const String baseUrl = 'http://10.202.23.23:8080/api';
```

**IP адрес компьютера в локальной сети:** `10.202.23.23`

---

## 🔧 ДВА СПОСОБА ЗАПУСКА

### Способ 1: На симуляторе (РЕКОМЕНДУЕТСЯ для тестирования)

**Преимущества:**
- ✅ Не требует сертификатов
- ✅ Быстрый запуск
- ✅ Все функции работают

**Команда:**
```bash
cd team_ai
flutter run -d "iPhone 16 Pro"
```

---

### Способ 2: На реальном iPhone (требует настройку)

**Для реального устройства нужно:**

#### 1. Настроить Xcode:
```bash
# Открыть проект в Xcode
cd team_ai
open ios/Runner.xcworkspace
```

#### 2. В Xcode:
1. Выбрать **Runner** в навигаторе слева
2. Выбрать **Runner** target
3. Перейти в **Signing & Capabilities**
4. Поставить галочку **Automatically manage signing**
5. Выбрать **Team** (войти с Apple ID если нужно)
6. Изменить **Bundle Identifier** на уникальный:
   ```
   Было: com.example.team_ai
   Стало: com.ваше-имя.teamai  (например: com.agybai.teamai)
   ```

#### 3. Подключить iPhone:
1. Подключить iPhone кабелем
2. Разрешить доверие на iPhone (Settings > General > Device Management)
3. В Xcode выбрать ваше устройство в списке устройств

#### 4. Запустить:
```bash
cd team_ai
flutter run
# Выбрать [1]: iPhone (Ағыбай)
```

---

## 🌐 ПРОВЕРКА ПОДКЛЮЧЕНИЯ

### 1. Убедиться что Backend запущен:
```bash
cd teamai-backend
./gradlew bootRun
```

### 2. Проверить доступность из локальной сети:
```bash
# На компьютере:
curl http://10.202.23.23:8080/api/actuator/health

# Должен вернуть:
# {"status":"UP"}
```

### 3. Убедиться что iPhone и компьютер в одной сети:
- Оба должны быть подключены к одному WiFi
- Проверить IP адрес компьютера:
  ```bash
  ipconfig getifaddr en0
  ```

---

## 🔥 БЫСТРЫЙ ТЕСТ

### После запуска приложения:

1. **Открыть регистрацию**
2. **Ввести данные:**
   - Имя: Test User
   - Email: mobile@test.com
   - Пароль: password123
   - Подтвердить: password123
3. **Нажать "Зарегистрироваться"**

### Если появится ошибка:
```
SocketException: Failed to connect...
```

**Решение:**
- Проверить что backend запущен
- Проверить что iPhone и компьютер в одной WiFi сети
- Проверить IP адрес компьютера:
  ```bash
  ipconfig getifaddr en0
  ```
- Обновить IP в `lib/services/api_service.dart` если изменился

---

## 🎯 ТЕСТИРОВАНИЕ ПОИСКА ПОЛЬЗОВАТЕЛЕЙ

### Сценарий на реальном устройстве:

1. **Зарегистрироваться / Войти**
   ```
   Email: ivan@gmail.com
   Password: password123
   ```

2. **Создать проект**
   - Название: Mobile Test
   - Описание: Testing on real device

3. **Добавить участника:**
   - Нажать "Добавить участника"
   - В поиск ввести: **"gmail"**
   - Должны появиться:
     - Ivan Petrov (ivan@gmail.com)
     - Maria Ivanova (maria@gmail.com)
     - И другие пользователи с @gmail.com

4. **Выбрать пользователя:**
   - Кликнуть на Maria Ivanova
   - Проверить что она добавлена в проект ✅

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема 1: "No development certificates"

**Решение:**
Использовать симулятор вместо реального устройства:
```bash
flutter run -d "iPhone 16 Pro"
```

### Проблема 2: "Connection refused"

**Причины:**
1. Backend не запущен
2. Неправильный IP адрес
3. iPhone и компьютер в разных сетях

**Решение:**
```bash
# 1. Проверить backend
curl http://localhost:8080/api/actuator/health

# 2. Проверить IP
ipconfig getifaddr en0

# 3. Обновить IP в api_service.dart
# 4. Перезапустить приложение
flutter run
```

### Проблема 3: "Пользователи не найдены"

**Причины:**
1. Backend не подключен
2. В базе нет пользователей
3. Токен авторизации не передается

**Решение:**
```bash
# 1. Создать тестовых пользователей
cd /Users/gibatolla/Production_Training/TeamAI_mob

# 2. Запустить скрипт создания пользователей
curl -X POST http://10.202.23.23:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Ivan Petrov",
    "email": "ivan@gmail.com",
    "password": "password123",
    "role": "USER"
  }'

curl -X POST http://10.202.23.23:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Maria Ivanova",
    "email": "maria@gmail.com",
    "password": "password123",
    "role": "USER"
  }'

# 3. Проверить поиск
# В приложении войти как ivan@gmail.com
# Искать "maria"
```

---

## 📊 ПРОВЕРКА ЛОГОВ

### В приложении (Flutter):
1. Открыть терминал где запущен `flutter run`
2. Смотреть логи запросов:
   ```
   [ApiService] POST /auth/register
   [ApiService] GET /users/search?email=gmail
   ```

### Backend логи:
```bash
# В терминале где запущен gradlew bootRun
# Искать строки:
# POST /api/auth/register
# GET /api/users/search
```

---

## ✅ ГОТОВО К ТЕСТИРОВАНИЮ

### Checklist:

- [x] Backend запущен (`./gradlew bootRun`)
- [x] Backend доступен по IP (`curl http://10.202.23.23:8080/api/actuator/health`)
- [x] IP адрес обновлен в `api_service.dart`
- [x] Тестовые пользователи созданы
- [x] Приложение запущено (симулятор или реальное устройство)
- [x] Регистрация работает
- [x] Поиск пользователей работает

---

## 🎉 ДЕМОНСТРАЦИЯ УЧИТЕЛЮ

### Подготовка:
1. Backend запущен
2. Создано 3-5 тестовых пользователей с разными email
3. Приложение запущено на симуляторе или реальном iPhone

### Сценарий:
1. **Показать регистрацию** нового пользователя
2. **Войти** в систему
3. **Создать проект**
4. **Добавить участника:**
   - Ввести "gmail" в поиск
   - Показать список найденных пользователей
   - Выбрать и добавить в проект
5. **Показать** список участников проекта ✅

---

**Дата:** 7 ноября 2025, 15:50  
**IP адрес компьютера:** 10.202.23.23  
**Backend URL:** http://10.202.23.23:8080/api  
**Статус:** ✅ ГОТОВО К ТЕСТИРОВАНИЮ

**ПОИСК ПОЛЬЗОВАТЕЛЕЙ БУДЕТ РАБОТАТЬ!** 🎉
